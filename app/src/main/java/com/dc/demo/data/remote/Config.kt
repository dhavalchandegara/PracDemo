package com.dc.demo.data.remote

object Config {

    const val topData = "/top_repository.json"

    const val middleData = "/middle_repository.json"

    const val bottomData = "/bottom_repository.json"

    const val category = "/category_repository.json"
}