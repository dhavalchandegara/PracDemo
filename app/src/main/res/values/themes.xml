<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DemoMVVM" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DemoMVVM" parent="Base.Theme.DemoMVVM" />

    <style name="Btn.OutLine" parent="Widget.Material3.Button.ElevatedButton">
        <item name="cornerFamily">rounded</item>
        <item name="cornerRadius">@dimen/_20sdp</item>
        <item name="android:background">@color/white</item>
        <item name="android:textColor">@color/black</item>
    </style>

</resources>